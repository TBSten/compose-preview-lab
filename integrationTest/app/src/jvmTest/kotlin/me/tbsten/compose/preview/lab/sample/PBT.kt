package me.tbsten.compose.preview.lab.sample

import io.kotest.core.Tag

/**
 * Tag for Property-Based Testing (PBT) tests.
 * These tests use forAll and are run in nightly CI builds.
 */
object PBT : Tag()
